#!/usr/bin/env bash

<% if @rails_env == "development" %>
cd <%= @rails_root %>
RACK_ENV=development DATABASE_HOST=<%= @db_host %> bundle exec rails server
<% else %>
<%= repo_root %>/scripts/deploy.sh rebuild_rails update_docs"
<% end %>
