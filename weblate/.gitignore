# Rendered on the instance at boot from SSM Parameter Store (see DEPLOYMENT.md).
# Contains the OIDC client secret, database and admin passwords, and the GitHub
# token — never commit it.
environment.prod
