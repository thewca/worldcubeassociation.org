type: object
properties:
  personId:
    type: integer
    example: 1
  ranking:
    type: integer
    example: 10
  attempts:
    type: array
    items:
      $ref: './WcifAttemptV2.yaml'
  best:
    $ref: './WcifAttemptResult.yaml'
  average:
    $ref: './WcifAttemptResult.yaml'
required:
  - personId
  - attempts
  - best
  - average
