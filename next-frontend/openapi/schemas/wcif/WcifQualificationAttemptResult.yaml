type: object
properties:
  whenDate:
    type: string
    format: date
  type:
    type: string
    example: "attemptResult"
  resultType:
    type: string
    enum: [ 'single', 'average' ]
  level:
    $ref: './WcifAttemptResult.yaml'
required:
  - whenDate
  - type
  - resultType
  - level
