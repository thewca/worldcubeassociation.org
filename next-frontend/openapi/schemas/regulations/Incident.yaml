type: object
# `resolved_at` is only set once an incident is published, and the `digest_*` and `private_*`
# fields are only serialized for users who may view Delegate matters. See
# `Incident::DEFAULT_PUBLIC_SERIALIZE_OPTIONS`.
required:
  - id
  - title
  - public_summary
  - created_at
  - updated_at
  - url
  - tags
  - competitions
properties:
  id:
    type: string
  title:
    type: string
  private_description:
    type: string
  private_wrc_decision:
    type: string
  public_summary:
    type: string
  created_at:
    type: string
    format: date-time
  updated_at:
    type: string
    format: date-time
  resolved_at:
    type: string
    format: date-time
  digest_worthy:
    type: boolean
  digest_sent_at:
    type: string
    format: date-time
  url:
    type: string
    format: uri
  tags:
    type: array
    items:
      type: object
      required:
        - name
        - content_html
        - url
      properties:
        name:
          type: string
        id:
          type: number
        url:
          type: string
          format: uri
        content_html:
          type: string
  competitions:
    type: array
    items:
      type: object
      required:
        - id
        - name
      properties:
        id:
          type: string
        name:
          type: string
        comments:
          type: string
