allOf:
  - $ref: '../competitions/BaseWcifRound.yaml'
  - type: object
    properties:
      results:
        type: array
        items:
          $ref: './RoundLiveResult.yaml'
      competitors:
        type: array
        items:
          $ref: './LiveCompetitor.yaml'
      round_id:
        type: number
      state_hash:
        type: string
      linked_round_ids:
        type: array
        items:
          type: string
      competitors_live_results_completed:
        type: number
    required:
      - results
      - competitors
      - round_id
      - state_hash
      - competitors_live_results_completed
