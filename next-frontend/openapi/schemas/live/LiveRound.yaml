allOf:
  - $ref: '../wcif/WcifRound.yaml'
  - type: object
    properties:
      results:
        type: array
        items:
          $ref: './LiveResult.yaml'
      competitors:
        type: array
        items:
          $ref: './LiveCompetitor.yaml'
      round_id:
        type: number
      state_hash:
        type: string
      linked_round_ids:
        type: array
        items:
          type: string
    required:
      - results
      - competitors
      - round_id
      - state_hash
