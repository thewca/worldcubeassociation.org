get:
  summary: Gets Live Admin Information
  tags:
    - live
  operationId: liveAdmin
  parameters:
    - name: competitionId
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Returns Live Admin Information with Rounds
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "../../../schemas/live/LiveRoundAdmin.yaml"
