get:
  summary: Get competitions for a person
  tags:
    - users
  operationId: personCompetitions
  parameters:
    - name: wca_id
      in: path
      required: true
      schema:
        type: string
  responses:
    '200':
      description: Successful response
      content:
        application/json:
          schema:
            $ref: '../../schemas/competitions/Competitions.yaml'
