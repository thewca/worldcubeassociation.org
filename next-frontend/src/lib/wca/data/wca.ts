export const WCA_API_PAGINATION = 25;

// Mirrors `Registration::DEFAULT_GUEST_LIMIT`, the cap that applies when a competition does not
//   restrict guests itself.
export const DEFAULT_GUEST_LIMIT = 99;

// Mirrors `Registration::COMMENT_CHARACTER_LIMIT`.
export const COMMENT_CHARACTER_LIMIT = 240;

// Money crosses the API in the currency's smallest unit (cents, pence, yen).
export const LOWEST_DENOMINATION_PER_UNIT = 100;
