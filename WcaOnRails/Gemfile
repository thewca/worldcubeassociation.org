source 'https://rubygems.org'

gem 'rails', '4.2.1'
gem 'sass-rails'
gem 'uglifier'
gem 'therubyracer'

gem 'sdoc', group: :doc

# fcgi can go once we're off of dreamhost.
gem 'fcgi'

gem 'jquery-rails'
gem 'turbolinks'
gem 'jbuilder'
gem 'bootstrap-sass'
gem 'mail_form'
gem 'simple_form'
gem 'valid_email'
gem "recaptcha", :require => "recaptcha/rails"

group :development, :test do
  gem 'byebug'
  gem 'sqlite3'
  gem 'web-console'
  gem 'spring'
  gem 'pre-commit'
end

group :production do
  # Use sqlite in production for now. We'll switch to mysql once we need to interact
  # with the database.
  # gem 'sqlite3'
end
