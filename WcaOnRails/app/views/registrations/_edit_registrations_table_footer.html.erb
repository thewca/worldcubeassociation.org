<tfoot>
  <tr>
    <td colspan="4">
      <%= render "registration_info_people", registrations: registrations %>
    </td>
    <% country_count = registrations.map(&:countryId).uniq.length %>
    <td><%= country_count %> <%= t('registrations.list.country').pluralize(country_count) %></td>
    <td></td>
    <% transposed = @registered_event_matrix[status].transpose %>
    <% @competition.events.each_with_index do |event, i| %>
      <td><%= transposed[i].reduce(0, :+) %></td>
    <% end %>
    <td></td>
    <td>
      <% if @competition.guests_enabled? %>
        <%# We need to convert `registrations` to an array here rather than computing the sum directly in the database.
            The reason is that we incude `registration_events` table which results in having duplicated registrations
            in the result set, thus the produced sum is too large.
            See: https://github.com/cubing/worldcubeassociation.org/issues/678#issuecomment-222386567 %>
        <%= registrations.to_a.sum(&:guests) %>
      <% end %>
    </td>
    <td colspan="3"></td>
  </tr>
</tfoot>
