<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <title>Map</title>
  <script>
    window.wca = {};
  </script>
  <%# FIXME: importing such a huge amount of assets just to display a map is ridiculous %>
  <%# we should extract our map methods to some standalone pack, not requiring jquery %>
  <%= javascript_packs_with_chunks_tag 'lodash_utils' %>
  <%= javascript_include_tag 'application' %>
  <%= stylesheet_link_tag 'application' %>
  <%= stylesheet_packs_with_chunks_tag 'application' %>
  <%= javascript_packs_with_chunks_tag 'application' %>
  <%= csrf_meta_tags %>
  <style>
    body {
      margin: 0px;
    }
    #map {
      /*
        Same hardcoded value as in 'app/helpers/markdown_helper.rb'.
        This avoids some bad rendering when the map is loaded with the tab content hidden.
      */
      width: 600px;
      height: 450px;
    }
  </style>
</head>
<body>
  <div id="map"></div>
  <script>
$(function() {
  let query = "<%= @query %>";
  let map = window.wca.createCompetitionsMapLeaflet("map");
  window.wca.searchAndPlaceOnMap(map, query);
});
  </script>
</body>
