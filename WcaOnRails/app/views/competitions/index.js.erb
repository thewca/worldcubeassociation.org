history.pushState(null, document.title, <%= current_path.to_json.html_safe %>);

$('#competitions-list').html('<%=j render 'index_competitions_list', competitions: @competitions %>');

$('#search-results').removeClass('competitions-list competitions-map competitions-admin').addClass('competitions-<%= @display %>');

$('#competition-query-form').removeClass('list admin map').addClass('<%= @display %>');
