<% provide(:title, @competition.name) %>

<%= render layout: 'nav' do %>
  <%= render "competition_info", competition: @competition %>

  <% if @competition.results_uploaded? %>
    <%= render "competition_results", results: @competition.winning_results, hide_pos: true, hide_round: true %>
  <% end %>
<% end %>
