# frozen_string_literal: true

class Rip_Feet < ActiveRecord::Migration[5.2]
  def up
    execute "DELETE FROM Events"
    load "#{Rails.root}/db/seeds/events.seeds.rb"
  end

  def down
    execute "DELETE FROM Events"
    load "#{Rails.root}/db/seeds/events.seeds.rb"
  end
end
