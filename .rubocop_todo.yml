# This configuration was generated by
# `rubocop --auto-gen-config`
# on 2016-04-15 15:39:38 +0200 using RuboCop version 0.36.0.
# The point is for the user to remove these configuration records
# one by one as the offenses are removed from the code base.
# Note that changes in the inspected code, or installation of new
# versions of RuboCop, may require this file to be generated again.

Metrics/CyclomaticComplexity:
  Exclude:
    - 'app/controllers/competitions_controller.rb'
    - 'app/controllers/registrations_controller.rb'
    - 'app/jobs/sync_mailing_lists_job.rb'
    - 'app/models/competition.rb'
    - 'lib/results_validators/*.rb'

Metrics/PerceivedComplexity:
  Exclude:
    - 'app/controllers/competitions_controller.rb'
    - 'app/controllers/registrations_controller.rb'
    - 'app/jobs/sync_mailing_lists_job.rb'
    - 'app/models/competition.rb'
    - 'lib/results_validators/*.rb'

# This cop wants us to stop doing things like
#   @being_cloned_from ||= Competition.find_by(id: being_cloned_from_id)
# because it thinks that `being_cloned_from_id` is a database column-backed property.
# But it isn't. We should think these few cases over, but that comes later.
Rails/FindByOrAssignmentMemoization:
  Exclude:
    - 'app/models/*.rb'
