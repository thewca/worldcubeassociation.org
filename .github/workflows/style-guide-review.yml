name: Style guide review

# Reviews pull requests against docs/STYLE_GUIDE.md and leaves inline comments.
# Advisory only: this workflow never fails a PR, it just comments.
#
# Runs OpenCodeReview (https://github.com/alibaba/open-code-review, Apache-2.0).
# The rules it applies live in .opencodereview/rule.json, which maps each area of
# the repo onto the relevant section of docs/STYLE_GUIDE.md. That file is the
# thing to edit when the guide changes -- OCR does not read STYLE_GUIDE.md
# itself, so the two must be kept in step by hand.
#
# Uses `pull_request_target` so that fork PRs (which is most contributions) can
# be reviewed at all -- `pull_request` runs from forks get no secrets, and this
# job needs an LLM token.
#
# Because `pull_request_target` runs in the context of the BASE repo with
# secrets available, this job must never execute code from the PR:
#   * no `yarn install`, no `bundle install`, no build, no tests
#   * the action checks out the trusted BASE branch and fetches the PR head as
#     bare git objects, so untrusted files are never materialised in the tree
#   * consequently .opencodereview/rule.json is always read from base, so a PR
#     cannot edit the rules it is about to be judged against
# Keep it that way when editing this file.
#
# Both the action and the npm package it installs are pinned. Bump them
# deliberately; `ocr_version: latest` would pull unreviewed code into a job that
# holds secrets.

on:
  pull_request:
    types: [opened, synchronize, ready_for_review]
    paths:
      - 'app/**'
      - 'lib/**'
      - 'spec/**'
      - 'db/**'
      - 'config/**'
      - 'next-frontend/**'
      - 'docs/STYLE_GUIDE.md'
      - '.opencodereview/rule.json'
      - '.github/workflows/style-guide-review.yml'

# A new push supersedes the review of the previous one.
concurrency:
  group: style-guide-review-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: write

jobs:
  review:
    # Draft PRs are still work in progress; don't spend tokens on them.
    if: github.event.pull_request.draft == false
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - name: Review against the style guide
        uses: alibaba/open-code-review@v1.8.6
        # Advisory, never a merge gate: a provider outage or a malformed diff
        # must not put a red X on a contributor's PR.
        continue-on-error: true
        with:
          ocr_version: '1.8.6'

          # Provider. See the note at the bottom of this file about Bedrock --
          # this must be an Anthropic- or OpenAI-shaped endpoint, and a Bedrock
          # runtime URL is neither.
          llm_url: ${{ secrets.OCR_LLM_URL }}
          llm_auth_token: ${{ secrets.OCR_LLM_AUTH_TOKEN }}
          llm_model: ${{ secrets.OCR_LLM_MODEL }}
          # 'true' for an Anthropic-shaped endpoint, 'false' for an
          # OpenAI-compatible one (which is what a LiteLLM proxy exposes).
          llm_use_anthropic: 'true'

          github_token: ${{ secrets.GITHUB_TOKEN }}

          # Update one summary comment in place rather than adding a new one on
          # every push, and only add inline comments on lines that don't already
          # have one -- otherwise a long-running PR accumulates duplicates.
          sticky_summary: 'true'
          incremental: 'true'

          # Keep the high bar: low-severity findings go to the summary instead
          # of interrupting the diff. Nothing is ever dropped.
          route_severity_below: 'low'

          # Raw model output is kept as a workflow artifact for debugging.
          upload_artifacts: 'true'

# ---------------------------------------------------------------------------
# Required secrets
#
#   OCR_LLM_URL         LLM endpoint
#   OCR_LLM_AUTH_TOKEN  bearer token for that endpoint
#   OCR_LLM_MODEL       model name
#
# On Bedrock: pointing OCR_LLM_URL straight at a bedrock-runtime invoke URL
# does not work, and the reason is the wire format rather than auth. A Bedrock
# API key would satisfy OCR's bearer auth fine, but:
#   * NewAnthropicClient appends "/v1/messages" to whatever URL it is given,
#     turning .../invoke into .../invoke/v1/messages;
#   * it then hands the base URL to the Anthropic SDK, which always POSTs to
#     {base}/v1/messages -- it cannot be routed to /model/{id}/invoke;
#   * it sends "model" in the request body, whereas InvokeModel takes the model
#     in the path and wants anthropic_version in the body instead.
#
# So reaching Bedrock needs something in front that speaks one of the two
# supported shapes:
#
#   1. Bedrock Access Gateway (aws-samples/bedrock-access-gateway) or LiteLLM,
#      exposing an OpenAI-compatible endpoint that signs to Bedrock. Point
#      OCR_LLM_URL at it and set llm_use_anthropic to 'false'. Keeps AWS
#      billing; costs us a service to run and keep available.
#   2. Use an Anthropic API key directly (llm_use_anthropic stays 'true').
#      Works immediately, but billing moves to the Anthropic console and out
#      of AWS.
# ---------------------------------------------------------------------------
