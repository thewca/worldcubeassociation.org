<% provide(:title, t('about.title')) %>

<%= react_component("StaticPagesAbout", props: {
  currentBoardMembers: UserGroup.board.flat_map(&:active_users).sort_by(&:name)
}, html_options: {
  class: 'container'
}) %>
