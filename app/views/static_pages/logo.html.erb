<% provide(:title, t('logo.title')) %>

<%= react_on_rails_component("StaticPagesLogo", props: {
  title: yield(:title),
  logoFileUrl: asset_url('/files/WCA Logo Assets.zip')
}, html_options: {
  class: 'container'
}) %>
