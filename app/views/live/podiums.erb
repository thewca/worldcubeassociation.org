<% provide(:title, 'WCA Live Integration Test Page') %>

<%= render layout: 'competitions/nav' do %>
  <%= react_component('Live/Podiums', { podiums: @results.as_json({ methods: [:podium, :event]}), competitors: @competitors.as_json({ methods: [:name, :user]}), competitionId: @competition.id, }) %>
<% end %>
