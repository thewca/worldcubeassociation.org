<% provide(:title, 'WCA Live Integration Test Page') %>

<%= render layout: 'competitions/nav' do %>
  <%= react_component('Live/Podiums', { podiums: @final_rounds.as_json({ only: [:event_id], methods: [:live_podium]}), competitors: @competitors.as_json({ methods: [:user]}), competitionId: @competition.id, }) %>
<% end %>
