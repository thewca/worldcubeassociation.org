<% provide(:title, t('registrations.import.title', comp: @competition.name)) %>

<%= render layout: "nav" do %>
  <%= react_component("ImportRegistrations", {
    competitionId: @competition.id,
  }) %>
<% end %>
