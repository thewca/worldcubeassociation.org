<% provide(:title, @competition.name) %>

<%= render layout: 'nav' do %>
  <%= react_on_rails_component("ManualPaymentSetup", props: {
    competitionId: @competition.id,
    accountDetails: @account_details,
  }) %>
<% end %>
