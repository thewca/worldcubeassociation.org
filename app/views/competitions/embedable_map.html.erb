<!DOCTYPE html>
<html>
<head>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <title>Map</title>
  <script>
    window.wca = {};
  </script>
  <%= javascript_pack_tag 'wca_maps', defer: false %>
  <%= stylesheet_pack_tag 'wca_maps' %>
  <%= csrf_meta_tags %>
  <style>
    body {
      margin: 0px;
    }
    #map {
      /*
        Same hardcoded value as in 'app/helpers/markdown_helper.rb'.
        This avoids some bad rendering when the map is loaded with the tab content hidden.
      */
      width: 600px;
      height: 450px;
    }
    .invisible-iframe-map {
      z-index: -1;
      position: absolute;
      width: 100%;
      height: 100%;
      top: 0;
      left: 0;
      border: 0;
    }
  </style>
</head>
<body>
  <div id="map"></div>
  <script>
  let query = "<%= @query %>";
  let map = window.wca.createCompetitionsMapLeaflet("map");
  window.wca.searchAndPlaceOnMap(map, query);
  </script>
</body>
