<% provide(:title, "Newcomer Checks") %>

<%= render layout: 'nav' do %>
  <h1><%= yield(:title) %></h1>

  <%= react_on_rails_component("NewcomerChecks", props: {
    competitionId: @competition.id,
  }) %>

<% end %>
