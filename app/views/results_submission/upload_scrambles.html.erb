<%= render layout: 'competitions/nav' do %>
  <%= react_on_rails_component("ScrambleMatcher", props: {
    wcifEvents: @competition.events_wcif,
    competitionId: @competition.id,
    initialScrambleFiles: @competition.scramble_file_uploads,
    matchedScrambleSets: @competition.matched_scramble_sets,
  }) %>
<% end %>
