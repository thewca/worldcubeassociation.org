development:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://wca_redis:6379/3" } %>

test:
  adapter: test

production:
  adapter: redis
  url: <%= ENV.fetch("REDIS_URL") { "redis://localhost:6379/1" } %>
  channel_prefix: wca_production
